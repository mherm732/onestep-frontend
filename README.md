# OneStep Frontend (Flutter Web)
Flutter web client for OneStep.

## Quickstart
- Flutter SDK installed
- `flutter pub get`
- Dev run:
  `flutter run -d chrome --dart-define=API_BASE_URL=http://localhost:8080`
- Build for web:
  `flutter build web --release`
